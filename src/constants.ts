export enum Inputs {
  Path = "path",
  Key = "key",
  RestoreKeys = "restore-keys",
}

export enum Outputs {
  CacheKey = "cache-key",
  CacheHit = "cache-hit",
  ExactHit = "exact-hit",
}

export const RefKey = "GITHUB_REF";
